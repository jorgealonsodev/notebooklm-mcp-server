{
  "mcpServers": {
    "notebooklm": {
      "command": "./notebooklm-mcp-server",
      "args": ["--transport=stdio", "--profile=full"],
      "env": {
        "HEADLESS": "true",
        "STEALTH_ENABLED": "true",
        "ANSWER_TIMEOUT_MS": "300000"
      }
    }
  }
}
